export default class PlaygroundUrl {

    openUItestingPPage() {
        return browser.url("http://uitestingplayground.com/");
    }

    urlOfProgressBarPage() {
        return browser.url("http://uitestingplayground.com/progressbar");
    }

    urlOfAjaxDataPage() {
        return browser.url("http://uitestingplayground.com/ajax");
    }

    urlOfClickPage() {
        return browser.url("http://uitestingplayground.com/click");
    }

    urlOfVisibilityPage() {
        return browser.url("http://uitestingplayground.com/visibility");
    }

    urlOfShadowDomPage() {
        return browser.url("http://uitestingplayground.com/shadowdom");
    }

}